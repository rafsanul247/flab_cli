name: flab
description: A CLI tool to scaffold Flutter projects with Clean Architecture, MVVM, MVC, and custom themes.
version: 1.0.4
repository: https://github.com/rafsanul247/flab_cli

environment:
  sdk: '>=3.0.0 <4.0.0'

executables:
  flab: flab

dependencies:
  args: ^2.5.0
  cli_dialog: ^0.5.0
  io: ^1.0.5
  path: ^1.9.0
  mason_logger: ^0.3.5

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0