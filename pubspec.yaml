name: ftool_cli
description: A CLI tool to scaffold Flutter projects with Clean Architecture, MVVM, MVC, and custom themes.
version: 1.0.0
repository: https://github.com/your_username/ftool_cli

environment:
  sdk: '>=3.0.0 <4.0.0'

executables:
  ftool: ftool_cli

dependencies:
  args: ^2.4.2
  cli_dialog: ^0.5.0
  io: ^1.0.4
  path: ^1.8.3
  mason_logger: ^0.2.11

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0